<h2>Страви з категорії: @ViewBag.CategoryName</h2>

<div class="recipe-grid">
    @foreach (var recipe in Model)
    {
        <div class="recipe-card">
            <h4>@recipe.recipe_name</h4>
            <!-- другие данные рецепта -->
        </div>
    }
</div>
